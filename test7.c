#include <stdio.h> 

int main() {
    // Integer variable to store whole numbers
    int integerVariable = 42;
    integerVariable = 2000;
    // Displaying the values stored in the variables
    printf("Integer Variable: %d\n", integerVariable);
    return 0;
} 
